# Test Plan

- Unit: validators, auth service password checks, AI payload formatting.
- Integration: register/login, create reading, processing polling, admin guard.
- Security checks: CSRF token required, upload file type/size limits, rate limit behavior.
- UI smoke checks: home page, pricing, wizard, result page in both `ar` and `en`.
